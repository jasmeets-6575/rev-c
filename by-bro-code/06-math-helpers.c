#include <stdio.h>
#include <math.h>

int main() {

    int x = 9;

    x = sqrt(x);

    // roundoff 
    // x = round(x);

    // x = ceil(x);
    // x = floor(x);

    // absolute ( +ve)
    // x = abs(x);

    // x = log(x); 
    // x = sin(x);
    // x = cos(x);
    // x = tan(x);

    printf("%d", x);

    return 0;
}